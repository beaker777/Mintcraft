dubbo:
  consumer:
    timeout: 3000
    check: false
  protocol:
    name: dubbo
    port: -1
  registry:
    address: nacos://${mintcraft.nacos.server.url}
    parameters:
      namespace: ${mintcraft.dubbo.nacos.namespace}
      group: ${mintcraft.dubbo.nacos.group}
  application:
    name: ${spring.application.name}
    qos-enable: true
    qos-accept-foreign-ip: false