spring:
  application:
    name: mint-craft-goods
  config:
    import: optional:classpath:base.yml, optional:classpath:datasource.yml, optional:classpath:rpc.yml, optional:classpath:cache.yml

server:
  port: 8099
